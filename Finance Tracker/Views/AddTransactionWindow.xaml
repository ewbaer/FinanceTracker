<Window x:Class="FinanceTracker.Views.AddTransactionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="" Height="251" Width="400"
        Background="#1e1e1e" Foreground="White" WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Title:"/>
            <TextBox x:Name="TitleBox"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Amount:"/>
            <TextBox x:Name="AmountBox"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="Date:"/>
            <DatePicker x:Name="DatePicker"/>
        </StackPanel>

        <Button Grid.Row="3" Content="Add" HorizontalAlignment="Right" Width="100" Click="Add_Click"/>
    </Grid>
</Window>
